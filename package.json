{
  "name": "codequest",
  "private": true,
  "scripts": {
    "install:all": "npm install --include=dev --prefix backend && npm install --include=dev --prefix front",
    "dev": "node scripts/dev.mjs",
    "prebuild": "npm run install:all",
    "build": "npm run build-only --prefix front && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  }
}
