{
  "name": "codequest",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix front",
    "dev": "node scripts/dev.mjs",
    "build": "npm run build-only --prefix front && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  }
}
